name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.11", "3.12", "3.13", "3.14"]
        include:
          # One Windows job for cross-platform coverage (primary dev env).
          - os: windows-latest
            python-version: "3.12"

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install
        run: uv pip install --system -e ".[dev]"

      - name: Test
        run: pytest -ra --cov=langstage_core --cov-report=term-missing

  minimal-install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install with NO extras (declared deps only)
        run: uv pip install --system .

      - name: Import smoke on a minimal install
        run: |
          python -c "import langstage_core; from langstage_core import load_agent_spec; from langstage_core.tasks import TaskRunner, TASK_TOOLS; from langstage_core.agui import build_agent; print('ok')"
